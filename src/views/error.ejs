<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Ticket Verification</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400..900&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 0;
      min-height: 100vh;
      color: #F7F7F7;
      background: radial-gradient(circle at top left, rgba(10, 10, 25, 1) 0%, rgba(5, 5, 15, 1) 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .error-container {
      background: rgba(25, 25, 35, 0.7);
      backdrop-filter: blur(15px);
      border-radius: 20px;
      padding: 40px;
      width: 100%;
      max-width: 500px;
      border: 1px solid rgba(239, 68, 68, 0.3);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.45);
      text-align: center;
    }

    .icon {
      font-size: 64px;
      margin-bottom: 20px;
    }

    h1 {
      color: #ef4444;
      font-weight: 800;
      margin-top: 0;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    p {
      color: #C0C0C0;
      line-height: 1.6;
      margin-bottom: 30px;
    }

    .error-code {
      display: inline-block;
      padding: 4px 12px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 6px;
      font-size: 11px;
      color: #666;
      font-family: monospace;
      margin-bottom: 30px;
    }

    .btn {
      display: inline-block;
      background: #C67509;
      color: white;
      padding: 12px 30px;
      border-radius: 10px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s;
    }

    .btn:hover {
      background: #e3860b;
      transform: translateY(-2px);
    }
  </style>
</head>

<body>
  <div class="error-container">
    <div class="icon">⚠️</div>
    <h1>
      <%= error %>
    </h1>
    <p>
      <%= message %>
    </p>
    <div class="error-code">CODE: <%= code %>
    </div>
    <div>
      <a href="/" class="btn">Return to Home</a>
    </div>
  </div>
</body>

</html>